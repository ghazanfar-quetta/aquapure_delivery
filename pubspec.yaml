name: aquapure_delivery
description: "Mineral Water Delivery App"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.1.12
  shared_preferences: ^2.2.2
  fl_chart: ^0.66.2


  
  # Firebase Services
  firebase_core: ^2.24.0
  cloud_firestore: ^4.9.0
  firebase_auth: ^4.13.0
  path_provider: ^2.1.1
  sqflite: ^2.3.0
  
  # HTTP for REST API
  http: ^1.1.0

  # State Management & Navigation
  provider: ^6.1.1
  go_router: ^12.1.3
  
  # UI & Design
  flutter_svg: ^2.0.9
  cached_network_image: ^3.3.0
  shimmer: ^3.0.0
  
  # Utilities
  intl: ^0.18.1
  uuid: ^4.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
 # flutter_launcher_icons: ^0.13.1

# flutter_launcher_icons:
 # android: true
 # ios: false
 # image_path: "assets/icon/icon.png"
 # min_sdk_android: 21
  

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icon/

